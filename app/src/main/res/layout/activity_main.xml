<?xml version="1.0" encoding="utf-8"?>
<!-- Legacy main activity layout (replaced by dashboard in current version) -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center">

    <!-- Button to trigger the 'SayHello' action -->
    <Button
        android:id="@+id/buttonSayHello"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:enabled="false"
        android:onClick="SayHello"
        android:text="text/Say Hello" />

    <!-- Area where the personalized greeting will be displayed -->
    <TextView
        android:id="@+id/textGreeting"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:text=""
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- Text input for the user's name -->
    <EditText
        android:id="@+id/nameText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:autofillHints="FirstName, LastName."
        android:hint="Enter Name Here"
        android:inputType="textPersonName"
        android:minHeight="48dp" />

</LinearLayout>